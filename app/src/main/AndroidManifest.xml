<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibreMan">

        <!-- LAUNCH ROLE SELECT AS MAIN ENTRY POINT -->
        <activity
            android:name=".RoleSelectActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ADMIN LOGIN -->
        <activity
            android:name=".AdminLoginActivity"
            android:exported="true" />

        <!-- ADMIN SIGNUP -->
        <activity
            android:name=".AdminSignupActivity"
            android:exported="true" />

        <!-- CATALOG -->
        <activity
            android:name=".CatalogActivity"
            android:exported="true" />

        <!-- SEARCH -->
        <activity
            android:name=".BookSearchActivity"
            android:exported="true" />

        <!-- BOOK DETAILS -->
        <activity
            android:name=".BookDetailActivity"
            android:exported="true" />

        <!-- MAIN ACTIVITY (DASHBOARD) – FOR FUTURE USE -->
        <!--
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        -->

    </application>

</manifest>